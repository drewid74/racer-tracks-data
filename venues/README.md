# venues/

One file per venue, named `venue-{stable_id}.json`. See
`../docs/VENUE_SCHEMA.md` for the exact JSON shape, `../docs/CONTRIBUTING.md`
for the PR flow, and `../docs/ATTRIBUTION.md` for the required attribution
strings per provenance.

Add or edit exactly one file per pull request.
